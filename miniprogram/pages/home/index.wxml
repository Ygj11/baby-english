<view class="page">
  <view class="hero">
    <text class="eyebrow">Baby English</text>
    <text class="title">Hi！今天想学什么？</text>
    <text class="subtitle">选一个开始吧</text>
    <text class="backend-status backend-status--{{backendStatus}}">
      Backend: {{backendStatus}}
    </text>
  </view>

  <t-cell-group theme="card" t-class="entry-list">
    <t-cell
      wx:for="{{entries}}"
      wx:key="id"
      title="{{item.label}}"
      url="{{item.url}}"
      arrow="{{!!item.url}}"
      hover="{{!!item.url}}"
    >
      <text slot="left-icon" class="entry-icon">{{item.icon}}</text>
    </t-cell>
  </t-cell-group>

  <view class="settings-entry" bind:tap="goToProfile">
    <text>⚙️ 学习设置</text>
    <text class="settings-entry__hint">年龄、年级与英语水平</text>
  </view>
</view>
